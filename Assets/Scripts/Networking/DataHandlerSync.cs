using Fusion;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataHandlerSync : MonoBehaviour
{
    //private const int DataHandlerSize = 1200;
    
    //[Networked(OnChanged = nameof(OnAvatarDataChanged)), Capacity(DataHandlerSize)] private NetworkArray<byte> AvatarData { get; }

    //static void OnAvatarDataChanged(Changed<DataHandlerSync> changed) => changed.Behaviour.ApplyAvatarData();

}
